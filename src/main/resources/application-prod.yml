logging:
  level:
    org.springframework: WARN

spring:
  datasource:
    url: jdbc:mariadb://checkout-db:3306/checkout
    password: ${DB_ROOT_PASS}
  application:
      zipkin:
        base-url: http://zipkin.monitoring:9411/
  kafka:
    consumer:
      bootstrap-servers: kafka.default:9092

client:
  url: http://dh4r4pvj.ga

store:
  url: http://store.dh4r4pvj.ga